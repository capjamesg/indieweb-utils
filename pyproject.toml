[build-system]
requires = ["setuptools>=42", "mf2py", "jwt"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
python_paths = ["src"]
testpaths = ["tests"]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
