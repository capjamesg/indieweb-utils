Content Utilities
=================

Add footnotes to a post
-----------------------

The `add_footnote_links()` function replaces `[n]` and `[^n]` with HTML `<a>` tags that link to each other.

.. autofunction:: indieweb_utils.add_footnote_links